# Emacs Config

(
Install some dependencies:

```bash
$ npm install -g tern
```
)

Install all necessary emacs packages:

* company
* (company-tern)
* counsel (contained in ivy?)
* doom-themes
* ivy
* magit
* projectile
* which-key
